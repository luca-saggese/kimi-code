---
"@moonshot-ai/kimi-code": minor
---

Add experimental LLM-generated session titles. When enabled via `KIMI_CODE_EXPERIMENTAL_LLM_SESSION_TITLE`, the first user prompt is sent to the default model to generate a concise descriptive title instead of truncating the prompt text.
